# ターゲットアーキテクチャ図

作成: 2026-06-03

## モジュール依存図

依存は常に下向き。

```mermaid
graph TD
    app["app<br/>MainActivity<br/>・薄いNavホスト（画面切り替えのみ）<br/>・ViewModelの生成・依存注入<br/>・初回起動チェック"]

    subgraph feature["feature（UI層）"]
        subgraph launcher["launcher（土台）"]
            homevm["HomeViewModel<br/>・UI状態・ナビゲーション管理<br/>・ページ／行／スロット操作の受け口"]
            ssvm["ShortcutSelectViewModel<br/>・ショートカット選択UI状態"]
        end
        subgraph screens["screens（互いに無依存）"]
            settingsvm["SettingsViewModel<br/>・設定UI状態管理"]
            othervm["Calendar / Memo / AllApps Screen<br/>・各画面のUI"]
        end
    end

    ui["ui<br/>・共有Composable（ダイアログ・テーマ）<br/>・feature固有ロジックは持たない"]

    subgraph usecase["usecase（ビジネスロジック層）　※新設"]
        addusecase["AddShortcutUseCase<br/>・空きスロット探索＋ショートカット保存<br/>・複数Repository操作を1トランザクションで完結"]
        defaultusecase["ApplyDefaultLayoutUseCase<br/>・インストール済みアプリ判定（PackageManager）<br/>・デフォルトレイアウトの定義・生成・保存"]
        deleteusecase["DeleteShortcutUseCase<br/>・Pinショートカット解除（LauncherApps）<br/>・関連データ削除の連携"]
        cleanupusecase["CleanupOrphanedPinsUseCase<br/>・孤立Pin検出（Repository）<br/>・Pin解除（ShortcutHelper）<br/>・削除（Repository）の連携"]
    end

    subgraph platform["platform（Android機能層）"]
        shortcuthelper["ShortcutHelper<br/>・LauncherApps操作（起動・Pin解除）<br/>・アプリアイコン取得<br/>・未インストールアプリ検出（PackageManager）"]
        billing["BillingManager / AdManager<br/>・課金フロー・広告表示"]
        backup["BackupManager<br/>・バックアップ・復元（FileProvider）"]
    end

    subgraph data["data（データアクセス層）"]
        shortcutrepo["ShortcutRepository<br/>・Shortcut / Placement / LayoutConfig のCRUD<br/>・StateFlow&lt;LayoutState&gt;（変更通知の単一窓口）<br/>・JSON ⇔ SharedPreferences"]
        settingsrepo["SettingsRepository<br/>・設定値の読み書き（SharedPreferences）"]
        memorepo["MemoRepository / CalendarRepository<br/>・メモ・カレンダーデータのアクセス"]
    end

    model["model（Android非依存）<br/>・ShortcutModel / LayoutModel / LayoutState<br/>・PremiumModel / MemoModel<br/>・ShortcutTypeRules / LinkShortcutRules<br/>・どの層からもimport可能な共通型"]

    navigation["navigation<br/>・NavDestination（sealed class）<br/>・feature間の直接依存を排除する中立型"]

    app --> feature
    app --> usecase
    feature --> usecase
    feature --> ui
    feature --> platform
    feature --> data
    feature --> model
    feature --> navigation
    usecase --> platform
    usecase --> data
    usecase --> model
    ui --> model
    data --> model
    platform --> model
    platform --> data
```

---

## 各層の責務

### model/
Android SDK に依存しない純粋な Kotlin のデータクラス・ドメインロジック。
どの層からも import できる共通の型定義。最下層として Android 環境への依存を持たない。

### navigation/
画面遷移先の型定義（`NavDestination` sealed class）。
feature 同士が直接依存せず、「どこへ行くか」を宣言するだけで済むよう中立的な共有型として独立させる。

### data/
SharedPreferences / ContentProvider によるデータ永続化・アクセスを担う `*Repository` クラス。
**CRUD と StateFlow の提供のみ**を行う。ビジネスロジックは持たない。
`ShortcutRepository` は `StateFlow<LayoutState>` を保持し、複数の ViewModel に対してレイアウト状態の単一の真実を提供する。

### platform/
Android SDK（`LauncherApps` / `PackageManager` / `FileProvider` / `BillingClient` 等）を直接扱うクラス。
`*Manager` / `*Helper` などの命名が目安。
「Android 機能の配管」を担い、ビジネスロジックは持たない。

### usecase/
**data と platform をまたぐビジネスロジック・オーケストレーションを担う層。**
複数の Repository 操作をまとめる、PackageManager と Repository を組み合わせる、といった処理をここに集約する。
feature と data/platform の間の緩衝材として、feature がデータ層・Android層の詳細を知らなくて済むようにする。

### feature/
ViewModel と Screen（Composable）のペア。
**ViewModel は UI 状態の管理に専念する**。ビジネスロジックは UseCase に委譲し、Android SDK は直接呼ばない。
feature 間の相互依存は禁止。画面遷移は `NavDestination` を宣言するだけ。

### ui/
feature をまたいで使う共有 Composable コンポーネントとテーマ定義。
feature 固有の UI ロジックはここに置かず、汎用部品のみを管理する。

### app（MainActivity）
nav-host として画面の差し替えを機械的に行うだけの薄い層。
ViewModel の生成と依存注入もここで行う。
