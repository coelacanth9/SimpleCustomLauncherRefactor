# パッケージ構成

## レイヤー依存関係

```mermaid
graph TD
    A[MainActivity]

    subgraph model["model　─　純粋データ・ロジック（Android非依存）"]
        M[" "]
    end

    subgraph navigation["navigation　─　画面定義"]
        N[" "]
    end

    subgraph data["data　─　永続化層（*Repository）"]
        D[" "]
    end

    subgraph platform["platform　─　Android機能層（*Manager 等）"]
        P[" "]
    end

    subgraph feature["feature　─　UI層（ViewModel + Screen）"]
        F[" "]
    end

    subgraph ui["ui　─　共有UIコンポーネント・テーマ"]
        U[" "]
    end

    A --> F
    A --> D
    A --> P
    A --> N

    F --> model
    F --> navigation
    F --> data
    F --> platform
    F --> ui

    platform --> model
    platform --> data

    data --> model

    ui --> model
```

## ファイル一覧

```mermaid
graph LR
    root["com.coelacanth9.simplecustomlauncher"]

    root --> MainActivity.kt

    root --> model/
    model/ --> SM["ShortcutModel.kt\nShortcutType / ShortcutItem"]
    model/ --> STR["ShortcutTypeRules.kt\nisInternal / shouldDeleteOnRemove"]
    model/ --> LKR["LinkShortcutRules.kt\nRAKUTEN_LINK_PACKAGE / isLink*"]
    model/ --> LM["LayoutModel.kt\nShortcutPlacement / RowConfig / HomeLayoutConfig"]
    model/ --> LS["LayoutState.kt\nLayoutState"]
    model/ --> PM["PremiumModel.kt\nPremiumSource / PremiumStatus"]
    model/ --> MM["MemoModel.kt\nMemoItem / MemoSettings"]

    root --> navigation/
    navigation/ --> ND["NavDestination.kt\nHome / ShortcutSelect / Settings\nCalendar / Memo / AllApps"]

    root --> data/
    data/ --> SR["ShortcutRepository.kt\nStateFlow中心・SharedPreferences"]
    data/ --> SET["SettingsRepository.kt\nSharedPreferences"]
    data/ --> MR["MemoRepository.kt\nSharedPreferences"]
    data/ --> CR["CalendarRepository.kt\nContentProvider"]
    data/ --> DLC["DefaultLayoutConfig.kt\nデフォルトレイアウト定義"]

    root --> platform/
    platform/ --> SH["ShortcutHelper.kt\nLauncherApps / PackageManager"]
    platform/ --> SI["ShortcutIntent.kt\nShortcutItem.toIntent()"]
    platform/ --> SRA["ShortcutReceiverActivity.kt\nINSTALL_SHORTCUT受信"]
    platform/ --> PERM["PermissionManager.kt"]
    platform/ --> BKP["BackupManager.kt\nFileProvider / Intent共有"]
    platform/ --> billing/
    billing/ --> BM["BillingManager.kt\nGoogle Play Billing"]
    billing/ --> BS["BillingState.kt\nBillingConnectionState / ProductInfo / PurchaseState"]
    billing/ --> BC["BillingConstants.kt\nプロダクトID定数"]
    billing/ --> PRM["PremiumManager.kt\nSharedPreferences（課金ドメイン）"]
    platform/ --> ads/
    ads/ --> AM["AdManager.kt"]

    root --> feature/
    feature/ --> launcher/
    launcher/ --> home/
    home/ --> HVM["HomeViewModel.kt"]
    home/ --> HSC["HomeScreen.kt"]
    home/ --> PVM["PremiumViewModel.kt"]
    launcher/ --> shortcutselect/
    shortcutselect/ --> SSVM["ShortcutSelectViewModel.kt"]
    shortcutselect/ --> SSSC["ShortcutSelectScreen.kt"]
    feature/ --> screens/
    screens/ --> settings/
    settings/ --> STVM["SettingsViewModel.kt"]
    settings/ --> STSC["SettingsScreen.kt"]
    screens/ --> memo/
    memo/ --> MOSC["MemoScreen.kt"]
    screens/ --> calendar/
    calendar/ --> CASC["CalendarScreen.kt"]
    screens/ --> allapps/
    allapps/ --> AASC["AllAppsScreen.kt"]

    root --> ui/
    ui/ --> theme/
    theme/ --> TH["Color.kt / Type.kt / Theme.kt"]
    ui/ --> components/
    components/ --> CO["AddRowDialog.kt\nCommonDialogs.kt\nConfirmDialogs.kt\nContactTypeDialog.kt\nFloatingExpandableButton.kt\nPageIndicator.kt\nPremiumLockOverlay.kt"]
```

## レイヤー責務

| パッケージ | 責務 | Android依存 |
|---|---|---|
| `model/` | 純粋データクラス・ドメインロジック | なし |
| `navigation/` | 画面遷移先の定義 | なし |
| `data/` | データ永続化（`*Repository`） | SharedPreferences / ContentProvider |
| `platform/` | Android機能ラッパー（`*Manager` 等） | あり |
| `feature/` | ViewModel + Screen（UI層） | Compose / ViewModel |
| `ui/` | 共有コンポーネント・テーマ | Compose |

## 依存方向ルール

- `model/` はどこにも依存しない
- `data/` は `model/` のみに依存
- `platform/` は `model/` と `data/` に依存
- `feature/` は `model/` `navigation/` `data/` `platform/` `ui/` に依存
- `feature/` 間の相互依存は禁止
